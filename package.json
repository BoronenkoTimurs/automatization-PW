{
  "name": "pw",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "report" : "npx playwright show-report",
    "codegen" : "npx playwright codegen",
    "regression" : "npx playwright test",
    "wait-page": "npx playwright test --grep @wait",
    "keyboard-page": "npx playwright test --grep @keyboard",
    "mouse-page": "npx playwright test --grep @mouse"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.47.2",
    "@types/node": "^22.7.4"
  }
}
